{
  "name": "canvas-snapshots-capture",
  "version": "1.0.0",
  "description": "Collects frames from canvas to create timelapses. See https://github.com/Al3xCalibur/CanvasReplace for informations on the canvas",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js"
  },
  "author": "Matthias4217",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "dotenv": "^8.2.0",
    "node-cron": "^2.0.3",
    "canvas": "^2.6.1"
  },
  "devDependencies": {}
}
